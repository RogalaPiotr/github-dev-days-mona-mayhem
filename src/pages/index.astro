<html lang="en">
<head>
<meta charset="utf-8" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<meta name="viewport" content="width=device-width" />
<meta name="generator" content={Astro.generator} />
<title>Mona Mayhem - GitHub Contribution Battle Arena</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet" />
<style>
:root {
	font-family: 'Press Start 2P', system-ui, sans-serif;
	color-scheme: dark;
	--bg: #0a0a1a;
	--panel: rgba(10, 10, 26, 0.88);
	--border: rgba(95, 239, 131, 0.2);
	--green: #5fed83;
	--purple: #8a2be2;
	--text: #ecf7ef;
	--muted: rgba(236, 247, 239, 0.78);
}

body {
	margin: 0;
	padding: 2rem 1rem 3rem;
	min-height: 100vh;
	background:
		radial-gradient(circle at top, rgba(138, 43, 226, 0.14), transparent 34%),
		radial-gradient(circle at bottom right, rgba(95, 239, 131, 0.12), transparent 26%),
		linear-gradient(180deg, #0a0a1a 0%, #070713 100%);
	color: var(--text);
	position: relative;
	overflow-x: hidden;
	isolation: isolate;
}

body::before,
body::after {
	content: '';
	position: fixed;
	inset: 0;
	pointer-events: none;
	z-index: 0;
}

body::before {
	background: repeating-linear-gradient(to bottom, rgba(255, 255, 255, 0.07) 0, rgba(255, 255, 255, 0.07) 1px, transparent 1px, transparent 5px);
	opacity: 0.03;
	mix-blend-mode: soft-light;
	animation: scanlines 8s linear infinite;
}

body::after {
	background: linear-gradient(90deg, rgba(95, 239, 131, 0.05), transparent 24%, transparent 76%, rgba(138, 43, 226, 0.06));
	opacity: 0.7;
}

main {
	max-width: 1050px;
	margin: 0 auto;
	position: relative;
	z-index: 1;
	padding: 1.5rem;
	background: linear-gradient(180deg, rgba(10, 10, 26, 0.88), rgba(10, 10, 26, 0.74));
	border: 1px solid var(--border);
	border-radius: 1.25rem;
	box-shadow: 0 20px 60px rgba(0, 0, 0, 0.45), 0 0 40px rgba(138, 43, 226, 0.08);
	backdrop-filter: blur(6px);
}

h1 {
	margin: 0 0 0.5rem;
	font-size: clamp(1.5rem, 3vw, 2.8rem);
	line-height: 1.4;
	letter-spacing: 0.08em;
	color: #f3fffa;
	text-shadow: 0 0 6px rgba(95, 239, 131, 0.68), 0 0 14px rgba(95, 239, 131, 0.44), 0 0 26px rgba(138, 43, 226, 0.3);
	animation: neon-pulse 1.2s ease-in-out infinite, title-flicker 4.8s linear infinite;
}

p {
	margin-top: 0;
	color: var(--muted);
	line-height: 1.8;
}

.battle-form {
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
	gap: 0.75rem;
	align-items: end;
	margin: 1.5rem 0;
}

label {
	display: flex;
	flex-direction: column;
	gap: 0.4rem;
	font-weight: 400;
	color: var(--text);
	animation: float-in 680ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
}

.battle-form label:nth-of-type(1) {
	animation-delay: 80ms;
}

.battle-form label:nth-of-type(2) {
	animation-delay: 180ms;
}

.battle-form button {
	animation: float-in 680ms cubic-bezier(0.2, 0.8, 0.2, 1) 280ms both;
}

input {
	border: 1px solid rgba(95, 239, 131, 0.18);
	border-radius: 0.75rem;
	padding: 0.95rem 0.9rem;
	font-size: 0.72rem;
	font-family: inherit;
	background: linear-gradient(180deg, rgba(18, 18, 48, 0.96), rgba(10, 10, 26, 0.92));
	color: var(--text);
	box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.03);
	transition: border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease;
}

input::placeholder {
	color: rgba(236, 247, 239, 0.38);
}

input:focus-visible {
	outline: none;
	border-color: rgba(95, 239, 131, 0.75);
	box-shadow: 0 0 0 3px rgba(95, 239, 131, 0.12), 0 0 24px rgba(95, 239, 131, 0.18);
	transform: translateY(-1px);
}

button {
	border: 0;
	border-radius: 0.8rem;
	padding: 1rem 1.1rem;
	font-size: 0.72rem;
	font-family: inherit;
	font-weight: 400;
	cursor: pointer;
	background: linear-gradient(135deg, var(--green), #27b863 48%, var(--purple));
	color: #06110a;
	box-shadow: 0 0 18px rgba(95, 239, 131, 0.22), 0 0 24px rgba(138, 43, 226, 0.2);
	transition: transform 160ms ease, box-shadow 160ms ease, filter 160ms ease;
	position: relative;
	overflow: hidden;
}

button::before {
	content: '';
	position: absolute;
	inset: -60% -35%;
	background: linear-gradient(120deg, transparent 35%, rgba(255, 255, 255, 0.35) 50%, transparent 65%);
	transform: translateX(-120%) rotate(12deg);
	animation: button-sheen 3.8s ease-in-out infinite;
	pointer-events: none;
}

button:hover,
button:focus-visible {
	transform: translateY(-2px) scale(1.01);
	box-shadow: 0 0 24px rgba(95, 239, 131, 0.42), 0 0 36px rgba(138, 43, 226, 0.28);
	filter: brightness(1.06);
	outline: none;
}

button[disabled] {
	opacity: 0.7;
	cursor: wait;
}

.status {
	min-height: 1.5rem;
	font-weight: 400;
	margin-bottom: 1rem;
	font-size: 0.72rem;
	letter-spacing: 0.04em;
}

.status.error {
	color: #ff7d8b;
	text-shadow: 0 0 14px rgba(255, 125, 139, 0.28);
}

.status.loading {
	color: var(--green);
	animation: loading-shift 1.6s ease-in-out infinite;
	text-shadow: 0 0 10px rgba(95, 239, 131, 0.46), 0 0 20px rgba(138, 43, 226, 0.24);
}

.results {
	display: grid;
	grid-template-columns: 1fr auto 1fr;
	gap: 1rem;
	align-items: center;
}

.player-card {
	position: relative;
	overflow: hidden;
	background: linear-gradient(180deg, rgba(18, 18, 48, 0.96), rgba(10, 10, 26, 0.92));
	border: 1px solid rgba(95, 239, 131, 0.16);
	border-radius: 1rem;
	padding: 1rem;
	box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.03), 0 0 28px rgba(0, 0, 0, 0.28);
}

.player-card::before,
.player-card::after {
	content: '';
	position: absolute;
	inset: 0;
	pointer-events: none;
}

.player-card::before {
	background: linear-gradient(115deg, transparent 0%, transparent 41%, rgba(255, 255, 255, 0.16) 50%, transparent 59%, transparent 100%);
	transform: translateX(-120%);
	animation: card-shine 7s linear infinite;
	opacity: 0.85;
}

.player-card::after {
	background: radial-gradient(circle at top left, rgba(95, 239, 131, 0.15), transparent 30%), radial-gradient(circle at bottom right, rgba(138, 43, 226, 0.12), transparent 32%);
	opacity: 0.65;
}

.player-card:hover {
	box-shadow: inset 0 0 0 1px rgba(95, 239, 131, 0.22), 0 0 28px rgba(95, 239, 131, 0.12), 0 0 36px rgba(138, 43, 226, 0.1);
}

.player-card h2 {
	margin: 0;
	font-size: 0.82rem;
	line-height: 1.6;
	color: #f6fff8;
	text-shadow: 0 0 8px rgba(95, 239, 131, 0.3);
}

.player-meta {
	margin: 0.25rem 0;
	font-size: 0.68rem;
	line-height: 1.8;
	color: var(--muted);
}

.contribution-grid {
	display: grid;
	grid-auto-flow: column;
	grid-auto-columns: 12px;
	grid-template-rows: repeat(7, 12px);
	gap: 4px;
	margin-top: 0.75rem;
	overflow-x: auto;
	padding-bottom: 0.25rem;
}

.day-square {
	width: 12px;
	height: 12px;
	min-width: 12px;
	min-height: 12px;
	position: relative;
	border-radius: 2px;
	cursor: default;
	transition: transform 120ms ease, box-shadow 120ms ease, filter 120ms ease;
}

.day-square::after {
	content: attr(data-tooltip);
	position: absolute;
	left: 50%;
	bottom: calc(100% + 8px);
	transform: translateX(-50%);
	padding: 0.35rem 0.45rem;
	border-radius: 0.35rem;
	background: rgba(10, 10, 26, 0.96);
	border: 1px solid rgba(95, 239, 131, 0.22);
	color: #fff;
	font-size: 0.55rem;
	line-height: 1.2;
	white-space: nowrap;
	opacity: 0;
	visibility: hidden;
	transition: opacity 120ms ease;
	pointer-events: none;
	z-index: 2;
}

.day-square:hover::after,
.day-square:focus-visible::after {
	opacity: 1;
	visibility: visible;
}

.day-square:hover,
.day-square:focus-visible {
	transform: translateY(-1px) scale(1.14);
	box-shadow: 0 0 8px currentColor, 0 0 18px rgba(95, 239, 131, 0.34), 0 0 24px rgba(138, 43, 226, 0.28);
	filter: brightness(1.18) saturate(1.1);
	z-index: 1;
}

.day-square:focus-visible {
	outline: 1px solid rgba(255, 255, 255, 0.6);
	outline-offset: 1px;
}

.vs-badge {
	display: inline-flex;
	align-items: center;
	justify-content: center;
	width: 44px;
	height: 44px;
	border-radius: 50%;
	background: linear-gradient(135deg, rgba(95, 239, 131, 0.9), rgba(138, 43, 226, 0.9), rgba(95, 239, 131, 0.9));
	background-size: 220% 220%;
	color: #06110a;
	font-family: inherit;
	font-size: 0.62rem;
	font-weight: 400;
	letter-spacing: 0.08em;
	animation: vs-glow 1.8s ease-in-out infinite;
	position: relative;
	overflow: hidden;
}

.vs-badge::before {
	content: '';
	position: absolute;
	inset: 6px;
	border-radius: inherit;
	border: 1px solid rgba(255, 255, 255, 0.2);
	box-shadow: inset 0 0 12px rgba(255, 255, 255, 0.1);
}

@keyframes scanlines {
	0% {
		transform: translateY(0);
	}

	100% {
		transform: translateY(6px);
	}
}

@keyframes neon-pulse {
	0%,
	100% {
		opacity: 1;
		transform: translateY(0);
		text-shadow: 0 0 6px rgba(95, 239, 131, 0.68), 0 0 14px rgba(95, 239, 131, 0.44), 0 0 26px rgba(138, 43, 226, 0.3);
	}

	50% {
		opacity: 0.88;
		transform: translateY(-1px);
		text-shadow: 0 0 10px rgba(95, 239, 131, 0.82), 0 0 22px rgba(95, 239, 131, 0.56), 0 0 34px rgba(138, 43, 226, 0.52);
	}
}

@keyframes title-flicker {
	0%,
	18%,
	22%,
	57%,
	100% {
		opacity: 1;
	}

	19%,
	21%,
	56% {
		opacity: 0.92;
	}

	20%,
	55% {
		opacity: 0.78;
	}
}

@keyframes loading-shift {
	0%,
	100% {
		color: var(--green);
		text-shadow: 0 0 10px rgba(95, 239, 131, 0.46), 0 0 18px rgba(95, 239, 131, 0.24);
	}

	50% {
		color: var(--purple);
		text-shadow: 0 0 10px rgba(138, 43, 226, 0.54), 0 0 18px rgba(95, 239, 131, 0.2);
	}
}

@keyframes card-shine {
	0% {
		transform: translateX(-120%);
	}

	100% {
		transform: translateX(120%);
	}
}

@keyframes float-in {
	0% {
		opacity: 0;
		transform: translateY(16px) scale(0.98);
	}

	100% {
		opacity: 1;
		transform: translateY(0) scale(1);
	}
}

@keyframes button-sheen {
	0%,
	55% {
		transform: translateX(-120%) rotate(12deg);
	}

	100% {
		transform: translateX(140%) rotate(12deg);
	}
}

@keyframes vs-glow {
	0%,
	100% {
		background-position: 0% 50%;
		box-shadow: 0 0 18px rgba(95, 239, 131, 0.36), 0 0 30px rgba(138, 43, 226, 0.26), 0 0 44px rgba(95, 239, 131, 0.12);
		transform: rotate(0deg) scale(1);
	}

	50% {
		background-position: 100% 50%;
		box-shadow: 0 0 28px rgba(95, 239, 131, 0.62), 0 0 48px rgba(138, 43, 226, 0.48), 0 0 68px rgba(95, 239, 131, 0.2);
		transform: rotate(-3deg) scale(1.08);
	}
}

@media (max-width: 860px) {
	.results {
		grid-template-columns: 1fr;
	}

	.vs-badge {
		justify-self: center;
	}
}

@media (prefers-reduced-motion: reduce) {
	*,
	*::before,
	*::after {
		animation-duration: 0.01ms !important;
		animation-iteration-count: 1 !important;
		transition-duration: 0.01ms !important;
		scroll-behavior: auto !important;
	}

	body::before {
		opacity: 0.08;
	}

	.player-card::before,
	button::before {
		display: none;
	}
}
</style>
</head>
<body>
<main>
<h1>Mona Mayhem - GitHub Contribution Battle Arena</h1>
<p>Compare two GitHub users and see who contributed more over the last year.</p>

<div class="battle-form">
<label for="player1">
Player 1
<input id="player1" name="player1" type="text" autocomplete="off" placeholder="octocat" />
</label>
<label for="player2">
Player 2
<input id="player2" name="player2" type="text" autocomplete="off" placeholder="torvalds" />
</label>
<button id="battleButton" type="button">Battle</button>
</div>

<p id="statusMessage" class="status" aria-live="polite"></p>

<section id="results" class="results" aria-live="polite"></section>
</main>

<script src="../scripts/battle.ts"></script>
</body>
</html>
